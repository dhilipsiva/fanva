# Rust build output (unanchored so nested workspace crates + fuzz/ are covered too)
target/

# Nix / direnv
result
result-*
.direnv/

# Cloudflare Worker (fanva-proxy) local state + secrets — never commit
.wrangler/
.dev.vars
.env

# Web build output / npm
dist/
node_modules/

# Fetched data: lensisku bulk export consumed by smuni-dictionary/build.rs
# (`just fetch-dict`; CC-BY-SA, 10MB — never commit)
dictionary-en.json
dictionary-en.json:Zone.Identifier

# Fuzzing state (`just fuzz-seed` regenerates the corpus from the .lojban corpora)
fuzz/corpus/
fuzz/artifacts/

# Python flywheel outputs + tooling
data/
models/
__pycache__/
.venv/

*.log

# Claude Code per-machine settings (personal permission grants)
.claude/settings.local.json
